import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dhehe.naru',
  appName: 'anime007',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
